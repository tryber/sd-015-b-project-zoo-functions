const { species } = require('../data/zoo_data');
const data = require('../data/zoo_data');

function arrayNW() {
  const arrayNW = [];
  const NW = species.filter((a) => {
    if
  })
}

function getAnimalsLocation() {
  const obj = {
    NE: arrayNW(),
    NW: [],
    SE: [],
    SW: [],
  };
  console.log(obj);
}

getAnimalsLocation();
function getAnimalMap(options) {

}

module.exports = getAnimalMap;

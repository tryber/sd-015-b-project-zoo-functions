// const { species } = require('../data/zoo_data');
// const data = require('../data/zoo_data');

// const NE = species.filter((specie) => specie.location === 'NE').map((specie) => specie.name);
// const NW = species.filter((specie) => specie.location === 'NW').map((specie) => specie.name);
// const SE = species.filter((specie) => specie.location === 'SE').map((specie) => specie.name);
// const SW = species.filter((specie) => specie.location === 'SW').map((specie) => specie.name);
// const fullNE = species.filter((specie) => specie.location === 'NE');
// const fullNW = species.filter((specie) => specie.location === 'NW');
// const fullSE = species.filter((specie) => specie.location === 'SE');
// const fullSW = species.filter((specie) => specie.location === 'SW');

// const NEwithNames = NE.reduce((ac, c, i,) => c. , {});
// console.log(NEwithNames);
// const NWwithNames = species.filter((specie) => specie.location === 'NW').map((specie) => specie.name);
// const SEwithNames = species.filter((specie) => specie.location === 'SE').map((specie) => specie.name);
// const SWwithNames = species.filter((specie) => specie.location === 'SW').map((specie) => specie.name);

// function getAnimalMap(options) {
//   const simpleMap = { NE, NW, SE, SW };
//   if (!options) return simpleMap;

//   if (options.includeNames === true);
// }

// console.log(getAnimalMap());

// module.exports = getAnimalMap;

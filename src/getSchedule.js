const data = require('../data/zoo_data');

function getSchedule(scheduleTarget) {
// retorna objeto com 'dia' dendo dele tem um objeto com 'officeHour'/hotario e 'exhibition'/animais.
// pegar dia;
// pegar horário;
// pegar animais que serão exibitos
}
console.log(getSchedule());

module.exports = getSchedule;

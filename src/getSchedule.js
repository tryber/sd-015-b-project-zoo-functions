const data = require('../data/zoo_data');

// function officeSchedule() {
//   const day = Object.keys(hours);
//   const sched = Object.values(hours);
//   const valueSched = sched.map((elemento) => `Open from ${sched.open}am until ${sched.close}pm`);
//   return valueSched;
// }

function getSchedule(scheduleTarget) {
  // seu código aqui
}

module.exports = getSchedule;

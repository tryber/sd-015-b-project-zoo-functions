const data = require('../data/zoo_data');

function isManager(id) {
  // seu código aqui

  // employees.some((pessoa) => pessoa.managers !== []);
}

function getRelatedEmployees(managerId) {
  // seu código aqui

}

module.exports = { isManager, getRelatedEmployees };
